// import { getData } from './api.js'

// export async function getPlatforms(subject, query, KEY) {
//     // //get from localstorage first
//     const localStoragePlatformList = localStorage.getItem('PLATFORM_LIST')
//     // //fetch data if localstorage doesn't exist
//     return localStoragePlatformList ? JSON.parse(localStoragePlatformList) : await getData(subject, query, KEY)
// }